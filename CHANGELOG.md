# 更新记录

## 1.4.0-dev.1 - 2026-09-17

- 基于正式版 `1.3.2` 开启下一阶段开发，插件版本与前端资源缓存标识更新为 `1.4.0-dev.1`。
- 生图页默认模型仅在页面加载时初始化，文生图与图生图分别保留最近选择的模型／工作流，切换模式或保存设置不再重置为默认模型。
- ComfyUI 显示快照批量应用允许上游提示词拼接来源变化，按显示节点、关联输出端口与通向所选保存节点的下游链路匹配，每张图片使用自己的文本；保留快照来源及冲突检查，普通文本候选与批量保存输出仍严格匹配。
- ComfyUI 可自动补全链路与来源明确的显示快照提示词，分别核验正反向和各采样阶段，标注“由显示快照识别”并保留来源；API 备用值、冲突快照和未知转换继续手动选择，不覆盖已解析或手动编辑内容。
- ComfyUI 常用文本节点、编码器、条件节点、显示节点和界面控件映射抽出为内置规则文件。导入页可点击文本节点条目，手动绑定字段输出、原样传递、明确顺序拼接或显示回写，预览解析后保存；正反向始终由管线判断，不自动反推或保存未知关系。
- 用户规则独立保存，默认限定当前工作流，校验端口结构、连接状态和控制项；规则管理支持删除。更改规则后重新解析本批图片，保留手填字段；取消导入不撤销已保存规则。
- ComfyUI 提交前按明确的字段映射同步本次修改到随图保存的界面工作流，覆盖固定输入、运行参数、随机种子和参考图文件名；固定编辑位置跨保存保留，仅修改当次副本，不刷新未修改的显示快照或画布。无法可靠映射时给出同步提示，生图仍可继续。
- 工作流同步兼容位置与具名两份控件值；核对运行回写后修正 PNG 工作流元数据，保留压缩像素数据。常见文本、模型加载与种子节点的适配集中到带来源依据的共用目录，区分读取、写回与随机策略；全局种子按明确契约核验，不猜测第三方控件顺序。
- ComfyUI 兼容性检查识别已验证的 Easy-Use 展示缓存和 WeiLin 编辑器按钮，消除“未声明输入”误报；这些字段被绑定、修改或接线时仍提示检查，未知字段和依赖错误继续保留。
- 图片解析器更新至 12，规则指纹纳入元数据、画廊和编辑缓存；已导入图片读取时按当前规则投影，维护时更新持久缓存，保留手动编辑参数。
- 存储升级为数据库 4-dev.1，迁移前自动备份，失败回滚；工作流修订、任务输出与图库共享压缩正文，API 图和界面图各保留一份精确表示，保持历史参数、64 位种子和人工编辑内容。
- ComfyUI 父子任务共享按内容保存的图片，图库落盘后复用图库原图；终态任务过期后归档大正文并保留幂等凭据，修正失败任务过期仍可恢复、未知父任务子结果被清理的边界。
- 队列和图库排序使用轻量状态与投影列，详情按需读取重型元数据；搜索只记录有效参数字段并去重，移除未使用的旧全文索引。
- 存储健康显示原图、缩略图、ComfyUI 缓存、数据库、备份和暂存等完整占用；启动、定时与手动维护共用清理流程，升级备份保留最近 3 份，深度维护在满足条件时回收数据库空闲空间。
- 设置保存后的信息刷新完成前保持保存按钮禁用，避免显示已保存但页面仍等待刷新的短暂状态中重复点击。

## 1.3.2 - 2026-09-17

优化手机和平板的大图浏览，修复切图闪回与图片条跟随延迟，并统一插件日志接口。

- 手机和平板的详情与灯箱按屏幕尺寸、像素密度加载显示图，普通切图不再自动读取整张原图；放大后按需加载原图，缩回后恢复显示图。下载与参考图继续使用原图。
- 显示图使用高质量缩小的 WebP，复用画廊预览与显示图缓存；后端限制编码并发与缓存容量，并重新核验外部文件状态。
- 灯箱闲时任务分次执行，详情图片升级避开正在进行的滑动；原图在后台线程转换为 Blob 后显示，兼容无 Worker 环境，关闭灯箱时回收临时资源。
- 双击按适应屏幕后的大小放大 2.5 倍，避免放大幅度随原图尺寸增大；解码缓存同时限制项数与像素内存。
- 修复详情滑动停稳后短暂闪回上一张图片：切图及时更新主图，仅同一张图片的清晰度升级等待手势结束。
- 灯箱图片条在连续滑动时及时跟随翻页目标，跨组保留玻璃背景；选中框与位置更新不再等待图片加载和动画停稳。
- 外部图库、图库存储和 ComfyUI 任务统一使用 AstrBot 提供的 logger，修正插件审核指出的日志接口用法。
- 插件版本与前端资源缓存标识统一为 `1.3.2`；沿用正式 v3 数据库，无新增结构迁移，兼容本阶段开发版数据。

## 1.3.1 - 2026-09-16

画廊先显示页面和卡片，再逐张加载图片，减少进入与翻页时整页等待的感觉。

- 进入画廊、翻页和切换筛选时立即显示卡片占位，不额外显示“正在读取”文字；翻页按钮和页码及时响应，允许连续切换，较早请求的结果不会覆盖新页面。
- 列表先读取图组信息，缩略图独立加载，优先显示可见及邻近卡片。加载中的图片使用线条占位图，不再误显示为“图片不可用”。
- 缩略图复用画廊与详情页的缓存和正在进行的请求；离开页面或翻页后停止旧页面尚未开始的加载，并限制并发请求数。
- 同页刷新保留已有卡片和图片；列表失败或单张预览失败均可重试，不阻塞其他卡片。
- 触屏平板在横竖屏下使用与手机相同的详情跟手滑动、跨图组切换及全屏灯箱，支持缩放和滑动退出；普通桌面鼠标设备保留原有预览方式。
- 手机和平板灯箱增加当前图组的缩略图条，点击主图与下载按钮一起显示或隐藏；可横向滚动并点选切图，下载按钮位于图片条上方，单图时隐藏图片条。图片条背景跟随主题的透明度与模糊效果。
- 插件版本与前端资源缓存标识更新至 `1.3.1`。沿用正式 v3 数据库，无新增结构迁移。

## 1.3.0 - 2026-09-16

新增 NovelAI 官方接口和 ComfyUI 工作流，支持从图库运行工作流、按名称查找模型，并整理代码结构与发布验证流程。

### NovelAI 官方接口

- 新增独立的「NovelAI 官方」服务商，使用 Persistent API Token 连接，保留原有 NAI 第三方接口。提供 V4.5／V5 Full、Curated 模型预设，按模型显示能力和默认参数。
- 支持文生图、基础图生图、多角色提示词、蒙版绘制与局部重绘；V4.5 提供角色／风格参考、Vibe Transfer 和 Variety Boost，V5 提供透明背景。V5 精选版局部重绘按官网行为使用 V4.5 精选版，并记录实际模型。
- Vibe 编码在运行期间缓存，参考图保留用途与顺序；执行前检查模型能力、参考组合及数量。V4.5 透明底图合成白底，V5 保留透明通道，局部重绘保留未遮罩区域。
- 同一 Token 跨服务商、跨模型共享串行限制，支持批次拆分与部分成功结果保留；超时不自动重发。兼容 JSON 和 ZIP 图片响应，逐图保存实际种子及参数，复制、详情与复现继续遵循参数行为设置。
- 标题栏显示订阅状态和 Anlas 余额，仅在选择 V5 时展示 V5 额度。免费试用需要验证码时明确提示；当前未接入试用验证码流程。
- PNG 支持读取 NovelAI Alpha 隐写参数，优先保留常规元数据并补齐缺失字段。图片解析器升级至 9，使用既有缓存回填流程。

官方接口已按文档和官网实现核对，并通过离线回归；真实账户的订阅查询已成功。受当前免费账户的验证码限制，成功生图、图生图、参考图效果及实际扣费仍待可用账户验证。

### ComfyUI 工作流

- 新增 ComfyUI 服务商，通过原生 HTTP／WebSocket 连接自建或反代服务器。可拖入 API JSON 或带工作流元数据的图片，选择结果节点、按需添加输入并明确替换模型；连接检查会列出缺失依赖，不自动安装节点或模型。
- 生图页先选择 ComfyUI 服务商，再选择工作流；没有主提示词绑定的固定工作流可直接运行。删除全部工作流后保存设置不再自动创建空条目。
- 画廊的「运行工作流」支持历史执行快照；未找到已保存工作流时，可选择服务商后临时使用，或添加到设置草稿。临时使用前检查兼容性，提交后的任务仍可恢复。
- 可配置「工作流单次出图张数」，默认 1；本次目标总张数 `count` 决定执行轮数，每轮超出截断、不足不补，结果合并为一个图组。该配置不修改或推断节点的 `batch_size`，执行同时遵守服务商与工作流并发限制。
- 任务独立于页面连接运行，可查询进度、恢复已有远端编号的任务及定向取消；提交状态未知时不自动重提。队列默认折叠，成功任务刷新后隐藏，异常任务保留并可手动清除。
- 导入后的输入默认为空，支持手动添加或自定义绑定，同一节点字段不能重复绑定。固定种子提示随编辑变化，已开放输入或设为 `-1` 时隐藏；清空固定种子后失焦自动改为 `-1`。提交前清除旧执行缓存标记，修复导入后重复得到旧图片的问题。
- 历史保留实际执行图、输入及执行计划，修改模板不影响旧任务；请求展示不再混入空项或内部任务编号。旧的生成张数绑定转换为普通输入，已提交旧任务按原执行策略恢复。

已在真实服务器完成基础文生图、图生图、多个输出节点及分批汇总测试，内网与 HTTPS 反代的查询、下载、WebSocket 均通过。任意第三方节点和复杂工作流仍需针对目标服务器检查依赖与验证。

### 工具与界面

- 能力工具新增服务商查询与模型／工作流搜索，支持名称、ID、用途说明、筛选和分页。指定模型查询使用 `model_refs` 字符串数组，逐项返回结果或错误；生图工具仍使用单个字符串 `model_ref`。
- 能力查询移除内部预览路径、临时保留期限和权限实现信息，保留资产复用、查看与发送指引。
- 所有服务商可独立配置 HTTP／HTTPS 网络代理，默认留空直连，覆盖生成、模型与额度查询、结果下载。启用开关移至名称右侧；服务商和模型／工作流列表支持拖拽排序，通过「保存全部设置」保存。
- 参数标题优先显示名称，参数键与说明放入统一玻璃风格提示层；手机端点击标题查看。移除重复气泡，长文本省略、参数解释及禁用原因等必要说明继续保留。

### 安装与升级

- 插件版本、注册信息与前端资源缓存标识统一为 `1.3.0`，本阶段开发记录合并至本正式版。
- 数据库升至正式 **v3**，新增工作流修订与任务表；开发期结构收拢为一次 `v2 → v3` 迁移，保留已发布的 `v1 → v2` 路径。新库直接创建正式 v3。
- 最终 `3-dev.1` 开发库核验后移除开发标记并转换，保留既有任务、修订与图像关联；继续支持历史 `1-dev.3`、`2-dev.2` 的受控转换。未知或不匹配的结构拒绝自动升级。
- 升级前自动备份数据库，失败回滚，重复启动不重复迁移。升级前仍需自行备份完整插件数据目录，自动数据库备份不包含配置和图片。
- 后端按服务商、图库、元数据、页面 API 和生成流程归组，前端分离弹窗、导入编辑与设置草稿；保留工具及页面接口约定。新增统一验证入口、隔离测试环境和安装包导入检查。

## 1.2.0 - 2026-09-13

完善工作流资产访问、图片导入和画廊管理，统一透明模糊主题，并优化桌面端与手机端的浏览体验。

- 工作流图片的会话访问关系与临时保留期限分离：原图仍存在时，已获准的会话可继续使用；临时保护固定为上次成功使用后的 1 小时。WebUI 移除时长配置，卡片改名为“预览图”。
- 沿用正式 v2 数据库结构，兼容 1.1.0 的数据和升级路径；本次发布不新增数据库迁移。
- 主题读取提前到页面头部启动；成功读取前保持页面主体不显示、背景透明，最多等待 4 秒后回退，超时后的迟到响应不再切换当前主题。
- 生成详情切图不再等待缩略图下载或图片解码；未加载图片使用占位显示，仍可连续滑动到其他图组。
- 生成详情支持用下一次触摸接管尚未结束的切图动画，保留当前图片位置，连续快滑不再丢失后续手势。
- 修复全屏灯箱快速切图时，仍在加载或重试的图片被误报为失败，以及加载期间出现黑色占位框的问题。
- 全屏灯箱的未加载图片显示半透明图片轮廓，亮色使用白色、深色使用灰色；占位随图片页移动，任一预览或原图显示后淡出，避免覆盖已显示的图片。
- 生成详情的滑动图片页及主图等待区域使用与灯箱一致的线条占位图，加载期间仍可连续切换图片。
- 主题色按钮、开关和滑块沿用侧边栏选中项的柔和配色，暗色开关使用灰色把手；统一单选、多选下拉的透明模糊材质及对钩颜色，去掉服务商和模型列表选中项的描边。
- 选中条和主题色控件改用经过颜色补偿的半透明背景，复用容器的模糊层，保持接近原来的深浅，并避免多选框重复叠色。
- 暗色主题的选中条、按钮、开关和滑块适度提亮，提升暗色背景上的辨识度。
- 单选、多选下拉菜单的不透明度统一随主题设置线性映射：主题 20%–100% 对应菜单 60%–100%，减少图片背景干扰。
- 分段切换组件的选中项与主题按钮共用半透明背景配色，包括生图模式、模型/工具配置和显示模式切换。
- 通知气泡统一使用下拉菜单的透明模糊材质，保留成功、错误通知的状态文字配色。

## 1.1.0 - 2026-09-12

新增外部图库和并发批量生图，完善导入整理、大图组浏览及手机端体验。

### 生图与参数

- 所有服务商支持按模型原生批次自动拆分多张请求，并受模型与服务商的共享并发上限约束。WebUI、指令和 Agent 使用同一套逻辑，部分失败时保留成功图片。
- 模型参数可分别配置是否在生图页显示、是否记录到历史、是否用于复现；省略开关时默认开启。隐藏、未记录或不回填的参数使用当前模型默认值。
- 反向提示词加入工具参数编辑列表，可设置是否向 Agent 开放、使用说明与工具默认值。图生图启停由支持开关控制，参考图数量下限统一为 1。
- 复现默认沿用当前生图数量和并发配置；NAI 风格与画师串冲突时以画师串为准。不支持生成的来源隐藏复现操作，保留原生参数导出。

### 画廊与图片导入

- 服务商、模式、调用来源和生图来源支持多选筛选；只列出有效选项，未知项放在最后。可将非空选择设为浏览器默认值，默认全选会自动包含新增选项。
- 搜索覆盖已记录的群聊、用户和平台信息；可按记录创建时间或图组最新图片时间排序。底部分页支持直接选择页码，手机端可横滑选择。
- 已导入图组支持重新编辑每张图片的参数并拖动排序，保留原图及内嵌元数据。
- ComfyUI 导入可识别主管线同一输出上的显示节点快照，优先使用工作流执行时回写的文本，并合并重复候选、隐藏已被下游完整覆盖的片段。
- 输出分支和提示词节点选择可批量应用到本次导入的匹配图片；每张图使用自己的内容，不匹配或人工改写的内容会跳过并说明原因。
- 大图组先显示目录和已有预览，再按需加载原图与参数；画廊、详情、编辑和灯箱复用缓存，跨组切换预加载相邻图片。

### 外部图库

- 在设置中添加 NAI 插件图库或自定义目录，支持多个来源、递归扫描、状态与进度查看；普通无元数据图片也可浏览。
- 原图保留在来源目录，Image Studio 管理索引与缩略图。外部图片不占自动生图配额，存储健康单独统计原图与缓存占用。
- 每个来源可独立配置收藏、删除、下载／导出和参考图权限。停用保留索引与收藏，移除只清除登记及缓存；删除外部原图前明确提示并确认。
- 外部收藏不能阻止来源插件清理原图；用作参考图时会保存独立副本。
- 按 NAI 文件名时间戳、图片元数据创建时间、btime、mtime 依次确定时间；自定义目录跳过文件名时间戳。读取异常会立即补充扫描，同一来源不重复排队。

### 界面与手机浏览

- 统一浮窗的玻璃材质、打开关闭动画和固定底栏，修复浮窗打开时侧栏与悬浮顶栏错位。
- 设置和详情的双列内容按累计高度交替排列；完善筛选菜单、分页栏、图标按钮及手机顶部栏布局。
- 主题与显示随「保存全部设置」保存，调整时即时预览；离开前可选择放弃未保存内容或留在设置页。
- 深色导航按主题色调整明暗层次，改善选中文字与图标的可读性。
- 手机灯箱保留连续滑动、捏合与双击缩放，改善切图背景闪白、背景更新卡住和长按系统菜单后的手势恢复；背景亮度继续跟随当前主题。

### 安装与升级

- 插件版本统一为 `1.1.0`，前端资源缓存标识同步更新。
- 数据库正式版本升至 **v2**；将外部图库开发修订收拢为一次 `v1 → v2` 升级，新库直接创建最终结构。
- 支持最终开发数据库 `2-dev.2` 校验后转换为正式 v2，保留 `1-dev.3` 的历史转换兼容；更早或未知开发结构拒绝自动转换。
- 升级前自动备份数据库，事务失败回滚，重复启动不重复备份。升级前仍需自行备份完整插件数据目录，自动数据库备份不包含图片和配置。

本阶段的开发更新已合并为 1.1.0 正式版记录。

## 1.0.0 - 2026-09-06

Image Studio 首个正式版本。

### 生图与 Agent

- 支持 OpenAI Images、Gemini、NAI 第三方 GET 和自定义 JSON 服务商。
- 服务商与模型分开管理，按模型能力提供文生图、图生图和参数默认值。
- 提供模型能力查询、生图、批量看图、产物投递四个 Agent 工具；可控制每个模型向 LLM 开放的能力和参数。
- 原图可作为后续改图参考，也可复制到 Agent 工作区继续处理，不依赖临时视觉预览。
- 支持 `/image_gen`、`/img` 指令和 `--help`；缺省参数使用模型默认值，显式错误模型不再静默回退。
- 指令参考图按当前消息、引用消息、参数顺序去重，按模型配置上限截断；支持 NAI 指令多张顺序生成。
- 生图页面显示所选 NAI 服务商的剩余额度和停用状态。

### 画廊与导入

- 提供搜索、来源和收藏筛选、自适应分页，以及批量收藏、导出和删除。
- 支持图组浏览和删除指定成员，复现当前图片参数、逐字段复制、原图下载。
- 支持 NovelAI、ComfyUI、Stable Diffusion 元数据导入，保留原始参数与工作流，并允许人工补充。
- ComfyUI 导入支持选择保存分支、查看采样阶段，以及将主管线候选文本加入提示词。
- 支持分别导入、作为图组导入和合并到同来源已有导入记录；重复文件去重，画廊已有图片整批拒绝重复上传。
- 导出包采用原图与同名 JSON 的平铺结构，下载响应完成后清理临时压缩包。

### WebUI 与数据

- 提供生图、画廊、导入、设置四个页面，支持桌面和手机布局。
- 手机端支持跟手滑动、跨图组连续切图、全屏浏览、双击与捏合缩放；优化图片加载与显示层交接。
- 新增浏览器独立主题设置：明暗模式、自选颜色、饱和度和玻璃透明度；桌面端支持吸色。
- 使用内容去重的图片资产存储，支持历史配额、收藏与导入豁免、参考图保留开关及来源身份记录。
- 定期检查资产、缩略图、引用和租约，提供手动存储健康检查。

### 安装与升级

- 最低要求更新为 **AstrBot 4.26.5、Python 3.12**，以匹配插件页面和工作区所需 API。
- 插件版本统一为 `1.0.0`，数据库收拢为正式 **v1**，删除开发期逐版本补列链路。
- 最终开发结构 `1-dev.3` 通过结构核验后，可自动备份数据库并单步转为 v1；旧开发结构、未知版本或结构不符时停止，不清空业务数据。
- 升级前应备份整个插件数据目录。自动数据库备份不包含原图、参考图或配置文件，不能替代完整备份。
- 作者更新为 **econeco**，项目地址为 https://github.com/Eco404/astrbot_plugin_image_studio 。

### 使用边界

- NAI 当前接入的是 `nai.sta1n.cn` 第三方协议，不是 NovelAI 官方 API，且仅支持文生图。
- ComfyUI 和 Stable Diffusion 当前用于导入归档与参数交换，不包含工作流执行器。
- 第三方模型能力和费用以服务商为准；NAI 指令多张请求中途失败时，已完成的请求可能已消耗额度。

开发期的中间修订已合并至此首发条目，不作为独立正式发布版本保留。
