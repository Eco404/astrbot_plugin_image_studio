# 更新记录

## 1.0.0 - 2026-09-06

Image Studio 首个正式版本。

### 生图与 Agent

- 支持 OpenAI Images、Gemini、NAI 第三方 GET 和自定义 JSON 服务商。
- 服务商与模型分开管理，按模型能力提供文生图、图生图和参数默认值。
- 提供模型能力查询、生图、批量看图、产物投递四个 Agent 工具；可控制每个模型向 LLM 开放的能力和参数。
- 原图可作为后续改图参考，也可复制到 Agent 工作区继续处理，不依赖临时视觉预览。
- 支持 `/image_gen`、`/img` 指令和 `--help`；缺省参数使用模型默认值，显式错误模型不再静默回退。
- 指令参考图按当前消息、引用消息、参数顺序去重，按模型配置上限截断；支持 NAI 指令多张顺序生成。
- 生图页面显示所选 NAI 服务商的剩余额度和停用状态。

### 画廊与导入

- 提供搜索、来源和收藏筛选、自适应分页，以及批量收藏、导出和删除。
- 支持图组浏览和删除指定成员，复现当前图片参数、逐字段复制、原图下载。
- 支持 NovelAI、ComfyUI、Stable Diffusion 元数据导入，保留原始参数与工作流，并允许人工补充。
- ComfyUI 导入支持选择保存分支、查看采样阶段，以及将主管线候选文本加入提示词。
- 支持分别导入、作为图组导入和合并到同来源已有导入记录；重复文件去重，画廊已有图片整批拒绝重复上传。
- 导出包采用原图与同名 JSON 的平铺结构，下载响应完成后清理临时压缩包。

### WebUI 与数据

- 提供生图、画廊、导入、设置四个页面，支持桌面和手机布局。
- 手机端支持跟手滑动、跨图组连续切图、全屏浏览、双击与捏合缩放；优化图片加载与显示层交接。
- 新增浏览器独立主题设置：明暗模式、自选颜色、饱和度和玻璃透明度；桌面端支持吸色。
- 使用内容去重的图片资产存储，支持历史配额、收藏与导入豁免、参考图保留开关及来源身份记录。
- 定期检查资产、缩略图、引用和租约，提供手动存储健康检查。

### 安装与升级

- 最低要求更新为 **AstrBot 4.26.5、Python 3.12**，以匹配插件页面和工作区所需 API。
- 插件版本统一为 `1.0.0`，数据库收拢为正式 **v1**，删除开发期逐版本补列链路。
- 最终开发结构 `1-dev.3` 通过结构核验后，可自动备份数据库并单步转为 v1；旧开发结构、未知版本或结构不符时停止，不清空业务数据。
- 升级前应备份整个插件数据目录。自动数据库备份不包含原图、参考图或配置文件，不能替代完整备份。
- 作者更新为 **econeco**，项目地址为 https://github.com/Eco404/astrbot_plugin_image_studio 。

### 使用边界

- NAI 当前接入的是 `nai.sta1n.cn` 第三方协议，不是 NovelAI 官方 API，且仅支持文生图。
- ComfyUI 和 Stable Diffusion 当前用于导入归档与参数交换，不包含工作流执行器。
- 第三方模型能力和费用以服务商为准；NAI 指令多张请求中途失败时，已完成的请求可能已消耗额度。

开发期的中间修订已合并至此首发条目，不作为独立正式发布版本保留。
